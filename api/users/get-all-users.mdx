---
title: "Get All Users"
api: "GET /api/users"
description: "Retrieve a list of all users in the system"
---

## Endpoint

```
GET /api/users
```

Retrieves all users registered in the Adoptme system.

## Response

<ResponseField name="status" type="string">
  Status of the request. Returns `"success"` on successful retrieval.
</ResponseField>

<ResponseField name="payload" type="array">
  Array of user objects.
  
  <Expandable title="User Object">
    <ResponseField name="_id" type="string">
      Unique identifier for the user
    </ResponseField>
    
    <ResponseField name="first_name" type="string">
      User's first name
    </ResponseField>
    
    <ResponseField name="last_name" type="string">
      User's last name
    </ResponseField>
    
    <ResponseField name="email" type="string">
      User's email address (unique)
    </ResponseField>
    
    <ResponseField name="password" type="string">
      User's hashed password
    </ResponseField>
    
    <ResponseField name="role" type="string" default="user">
      User's role in the system
    </ResponseField>
    
    <ResponseField name="pets" type="array" default="[]">
      Array of pet references associated with the user
      
      <Expandable title="Pet Reference">
        <ResponseField name="_id" type="string">
          Reference to a Pet document ID
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

## Example Request

```bash
curl -X GET https://api.adoptme.com/api/users
```

## Example Response

<ResponseExample>
```json Success Response
{
  "status": "success",
  "payload": [
    {
      "_id": "507f1f77bcf86cd799439011",
      "first_name": "John",
      "last_name": "Doe",
      "email": "john.doe@example.com",
      "password": "$2b$10$...",
      "role": "user",
      "pets": [
        {
          "_id": "507f191e810c19729de860ea"
        }
      ]
    },
    {
      "_id": "507f1f77bcf86cd799439012",
      "first_name": "Jane",
      "last_name": "Smith",
      "email": "jane.smith@example.com",
      "password": "$2b$10$...",
      "role": "user",
      "pets": []
    }
  ]
}
```

```json Error Response
{
  "error": "Ha ocurrido un error en la petición, por favor ver detalle: [error details]"
}
```
</ResponseExample>

## Error Responses

<ResponseField name="500" type="Internal Server Error">
  Returns an error message when the request fails due to a server error.
</ResponseField>